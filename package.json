{
  "name": "image-platform-check",
  "version": "0.2.0",
  "private": true,
  "description": "GitHub Action and reusable Renovate branch gate for OCI image platform availability.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.js",
    "check": "npm run typecheck && npm run build && npm test && git diff --exit-code -- dist"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
