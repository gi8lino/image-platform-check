{
  "name": "image-platform-check",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that verifies changed container images provide required OCI platforms.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.js",
    "check": "npm run typecheck && npm run build && npm test && git diff --exit-code -- dist"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
